const cunha = 'crie um backend crud com 3 tabelas relacionadas, Cliente, Pedido e Produto. o backend deve seguir as seguintes regras: cada tabela deve ter um controller que está dentro de backend/controllers, deve ter uma pasta chamada db que dentro terá o código de conexão com o sequelize e o db, e também deve ter uma pasta chamado model, que terá o script de criação das 3 tabelas (sequelize) e uma de relacionamento chamado rel.js. o index deve conter as rotas em routes. O site deve ter um sistema de login utilizando jwt e bcrypt. também deve conter as seguintes dependencias obrigatoriamente (utilize-as): express, sequelize, mysql2, nodemon e cors. utilize a pasta service caso necessário'